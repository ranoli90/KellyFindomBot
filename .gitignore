# Secrets
.env
*.secret

# Logs & Data
logs/
data/
*.log
*.db

# Backups
backups/
*.save*
*.bak

# Python
__pycache__/
*.pyc
venv/
.venv/

# IDE
.vscode/
.idea/
.claude/

# Test outputs
test_generations/

# Session files (Telethon)
*.session
*.session-journal

# Temp/artifacts
nul

# Archive (old copies)
archive/

# User data (runtime — generated by bot, do not commit)
ai_disclosure_shown.json
tip_history.json
blocked_users.json
csam_flags.json
reengagement_history.json
last_shutdown.json
golden_set.jsonl

# Chat logs
chat_logs/

# Creator persona (your private personality file)
heather_personality.yaml
heather_stories.yaml

# Media (creator content — provide your own)
images_db/
videos/
Heather.jpg
*.mp4
*.wav
*.mp3
*.jpg
*.png
*.zip

# Model files (too large for git)
*.gguf
*.bin
*.safetensors

# ─── AWS / Terraform ─────────────────────────────────────────────────────────
# NEVER commit these — they contain real secrets or account-specific data
infrastructure/terraform/terraform.tfvars
infrastructure/terraform/.terraform/
infrastructure/terraform/.terraform.lock.hcl
infrastructure/terraform/tfplan
infrastructure/terraform/*.tfstate
infrastructure/terraform/*.tfstate.backup
infrastructure/terraform/crash.log

# AWS credentials (should be in ~/.aws — never in repo)
.aws/
credentials

